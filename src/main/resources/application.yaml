server:
  port: 8080
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:

  # 업로드 용량 설정
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB